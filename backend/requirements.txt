# SITI Intelligence v5.0 — Backend Dependencies
# Entry point: gunicorn backend.server:app
# DO NOT add fastapi — it conflicts with flask-cors

flask==3.0.3
flask-cors==4.0.1
gunicorn==22.0.0

# Data processing
pandas==2.2.2
numpy==1.26.4

# HTTP client (Cashfree, OpenRouter)
httpx==0.27.2

# Environment
python-dotenv==1.0.1

# Supabase (optional — gracefully skipped if env vars not set)
supabase==2.4.2

# Twilio SMS / WhatsApp (optional — gracefully skipped)
twilio==9.2.3

motor==3.3.2
