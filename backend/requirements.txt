# SITI Intelligence Kernel — requirements.txt
# -----------------------------------------------
# Pinned for reproducibility on Render Free Tier.
# DO NOT add emergentintegrations or razorpay — they cause build failures.
# All external calls use httpx (async-safe, no requests library).

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0
gunicorn==22.0.0

# Async HTTP client (replaces requests everywhere)
httpx==0.27.0

# Supabase client
supabase==2.4.2

# Data validation
pydantic==2.7.1

# Async support
anyio==4.3.0

# Production ASGI server extras
python-multipart==0.0.9
